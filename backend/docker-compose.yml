version: '3.8'

services:
  connect-four-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/game.db
    volumes:
      - ./game.db:/app/game.db
    restart: unless-stopped